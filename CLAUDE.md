# CLAUDE.md

**Before every task:** Read the installed `agents/AGENTS.md` and this repo's `AGENTS.md`.
Open your response by stating the active flow and phase — or that no flow
applies (pure question/analysis, or edits to the agents repo itself) — and why.

The base instruction set lives in the [an-dr/agents](https://github.com/an-dr/agents)
clone installed globally by its `agents-install` skill — this repository carries no
`agents/` submodule. See `AGENTS.md`, which extends that base with repo-specific context.
